import { SpeedConversionPipe } from './speed-conversion.pipe';

describe('SpeedConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new SpeedConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
